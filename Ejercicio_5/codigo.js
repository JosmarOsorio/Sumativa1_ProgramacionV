//Escribir una expresión que involucre un string y un número y devuelva FALSE. Por: Josmar osorio. Para: Programación V-UBA

let string = "Hola"; //Creamos una variable con un dato de tipo string
let num = 777; //Creamos una variable con un dato de tipo number

console.log(string == num) //Comparamos ambas variables y obtenemos que es FALSE ya que no son iguales